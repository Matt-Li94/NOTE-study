```cpp

set SQL_SAFE_UPDATES = 0;

create table `students`(
	`student_id` int primary key,
    `name` varchar(30) not null,
    `major` varchar(30) unique,
    `score` int  
);

insert into `students` value(1,'lbh','Match',131);
insert into `students` value(2,'lbh','Chinese',124);
insert into `students` value(3,'lbh','English',134);
select * from `students`;

update `students`
set `major` = '英语' 
where `major` = 'English'; //在满足where条件的情况修改表格的信息

delete from `students`
where `student_id` = 2 or `major` = 'Match'; //在满足where条件的情况删除表格的信息

select * from `students` order by `score`; //查找信息，但是按照分数升序排序 没有写的时候默认就是ASC
select * from `students` order by `score` desc;//查找信息，但是按照分数降序排序 DESC
select * from `students` order by `score` limit 1;//查找信息，但是按照分数升序排序且只返回一条信息

-- 1.取得所有员工的资料-- 
select * from `employee`;
-- 2.取得所有客户的资料-- 
select * from `client`;
-- 3.按照薪水从低到高取得员工的资料-- 
select * from `employee` order by `money`;
-- 4.按照薪水从高到低取得前三名员工的资料-- 
select * from `employee` order by `money` desc limit 3;
-- 5.取得员工的名字-- 
select `name` from `employee`;
-- 1.取得所有员工的资料-- 
select * from `employee`;
-- 2.取得所有客户的资料-- 
select * from `client`;
-- 3.按照薪水从低到高取得员工的资料-- 
select * from `employee` order by `money`;
-- 4.按照薪水从高到低取得前三名员工的资料-- 
select * from `employee` order by `money` desc limit 3;
-- 5.取得员工的名字-- 
select `name` from `employee`;
```